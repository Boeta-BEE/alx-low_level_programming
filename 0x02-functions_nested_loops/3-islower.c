#include "main.h"


/**
 * _islower - checks lowercase
 * @c: character thats checked
 * Return: 1 is lowercase or 0 is everything else
 */

int _islower(int c)

{

	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
